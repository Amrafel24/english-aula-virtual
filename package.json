{
  "name": "english-aula-virtual",
  "version": "1.0.0",
  "private": true,
  "description": "English con HTML, CSS y JavaScript puro. Aula virtual con Node/SQLite o Sites/D1/R2.",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node servidor.js",
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build-aula.cjs",
    "db:generate": "drizzle-kit generate --config=drizzle.config.js"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2",
    "esbuild": "0.28.2"
  }
}
